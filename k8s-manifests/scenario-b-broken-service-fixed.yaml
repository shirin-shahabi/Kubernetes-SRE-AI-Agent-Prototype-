# Fixed version of broken service
apiVersion: v1
kind: Service
metadata:
  name: broken-service
  namespace: default
spec:
  selector:
    app: broken-service-app
    version: v1  # Fixed - now matches pod labels
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
